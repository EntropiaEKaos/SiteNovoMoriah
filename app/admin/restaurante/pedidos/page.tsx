import {prisma} from "../../../../lib/prisma";import {requireAdmin} from "../../../../lib/admin-auth";import {setRestaurantOrderStatus} from "../../../../lib/restaurant-actions";export const dynamic="force-dynamic";const money=(v:number)=>(v/100).toLocaleString("pt-BR",{style:"currency",currency:"BRL"});export default async function Page(){await requireAdmin();const orders=await prisma.restaurantOrder.findMany({where:{status:{in:["NEW","PREPARING","READY"]}},include:{items:true},orderBy:{createdAt:"asc"}});return <main style={{padding:"45px 5vw",background:"#f5f5f3",minHeight:"100vh"}}><small>MORIAH FOOD / KDS</small><h1 style={{fontSize:58}}>Cozinha</h1><section style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(280px,1fr))",gap:14}}>{orders.map(o=><article key={o.id} style={{background:o.status==="NEW"?"#fff8cf":"#fff",border:"1px solid #ddd",borderRadius:18,padding:20}}><small>{o.createdAt.toLocaleTimeString("pt-BR",{hour:"2-digit",minute:"2-digit"})} • {o.status}</small><h2>{o.guestName}</h2><p>{o.roomLabel||"Retirada/recepção"}</p>{o.items.map(i=><div key={i.id} style={{display:"flex",justifyContent:"space-between"}}><b>{i.quantity}× {i.nameSnapshot}</b><span>{money(i.totalCents)}</span></div>)}<h3>Total {money(o.totalCents)}</h3>{o.notes&&<p style={{background:"#f2f2f2",padding:10}}>Obs.: {o.notes}</p>}<form action={setRestaurantOrderStatus}><input type="hidden" name="id" value={o.id}/><select name="status" defaultValue={o.status}><option value="NEW">Novo</option><option value="PREPARING">Preparando</option><option value="READY">Pronto</option><option value="DELIVERED">Entregue</option><option value="CANCELLED">Cancelar</option></select><button style={{background:"#111",color:"#fff",padding:10}}>Atualizar</button></form></article>)}</section></main>}