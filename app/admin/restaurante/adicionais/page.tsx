import {prisma} from "../../../../lib/prisma";import {requireAdmin} from "../../../../lib/admin-auth";import {createModifierGroup,createModifierOption,linkModifierGroup} from "../../../../lib/restaurant-actions";export const dynamic="force-dynamic";const money=(v:number)=>(v/100).toLocaleString("pt-BR",{style:"currency",currency:"BRL"});export default async function Page(){await requireAdmin();const [groups,products]=await Promise.all([prisma.restaurantModifierGroup.findMany({include:{options:true,products:{include:{product:true}}},orderBy:{name:"asc"}}),prisma.restaurantProduct.findMany({where:{active:true},orderBy:{name:"asc"}})]);return <main style={{padding:"48px 5vw",maxWidth:1200,margin:"0 auto"}}><small>MORIAH FOOD / PERSONALIZAÇÃO</small><h1 style={{fontSize:58}}>Adicionais & Combos</h1><section style={{display:"grid",gridTemplateColumns:"repeat(3,1fr)",gap:14,margin:"24px 0"}}><form action={createModifierGroup} style={{padding:20,background:"#111",color:"#fff",borderRadius:16,display:"grid",gap:8}}><h3>Novo grupo</h3><input name="name" required placeholder="Escolha o ponto / Adicionais"/><input name="maxSelect" type="number" min="1" defaultValue="1"/><label><input name="required" type="checkbox"/> Obrigatório</label><button>Criar</button></form><form action={createModifierOption} style={{padding:20,background:"#fff7cc",borderRadius:16,display:"grid",gap:8}}><h3>Novo adicional</h3><select name="groupId" required><option value="">Grupo</option>{groups.map(g=><option key={g.id} value={g.id}>{g.name}</option>)}</select><input name="name" required placeholder="Bacon extra"/><input name="price" placeholder="Preço R$"/><button>Adicionar</button></form><form action={linkModifierGroup} style={{padding:20,background:"#fff",border:"1px solid #ddd",borderRadius:16,display:"grid",gap:8}}><h3>Vincular ao produto</h3><select name="productId" required><option value="">Produto</option>{products.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><select name="groupId" required><option value="">Grupo</option>{groups.map(g=><option key={g.id} value={g.id}>{g.name}</option>)}</select><button>Vincular</button></form></section>{groups.map(g=><article key={g.id} style={{padding:20,borderBottom:"1px solid #ddd"}}><h2>{g.name}</h2><small>{g.required?"Obrigatório":"Opcional"} • até {g.maxSelect}</small><p>{g.options.map(o=>o.name+" (+"+money(o.priceCents)+")").join(" • ")||"Sem opções"}</p><p><b>Produtos:</b> {g.products.map(x=>x.product.name).join(", ")||"nenhum"}</p></article>)}</main>}