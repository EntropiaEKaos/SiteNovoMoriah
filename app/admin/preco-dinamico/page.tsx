import {prisma} from "../../../lib/prisma";import {requireAdmin} from "../../../lib/admin-auth";import {createRateRule,deleteRateRule} from "../actions";export const dynamic="force-dynamic";
export default async function Page(){await requireAdmin();const [rules,rooms]=await Promise.all([prisma.rateRule.findMany({include:{accommodation:true},orderBy:[{priority:"asc"},{createdAt:"desc"}]}),prisma.accommodation.findMany({where:{active:true},orderBy:{name:"asc"}})]);return <main style={{padding:"50px 6vw",maxWidth:1100,margin:"0 auto"}}><small>MORIAH CMS / REVENUE AUTOMATION</small><h1 style={{fontSize:48}}>Preço dinâmico</h1><p>Automatize ajustes por ocupação real e antecedência. Valores positivos aumentam; negativos reduzem.</p><form action={createRateRule} style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(150px,1fr))",gap:10,padding:20,border:"1px solid #ddd",borderRadius:14,margin:"24px 0"}}><select name="accommodationId" required><option value="">Hospedagem</option>{rooms.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select><input name="name" required placeholder="Ex.: Alta ocupação"/><select name="adjustmentType"><option value="PERCENT">Percentual %</option><option value="FIXED">Valor fixo R$</option></select><input name="adjustmentValue" type="number" step="0.01" required placeholder="Ajuste: 15 ou -10"/><input name="minOccupancyPct" type="number" min="0" max="100" placeholder="Ocupação mín. %"/><input name="maxOccupancyPct" type="number" min="0" max="100" placeholder="Ocupação máx. %"/><input name="daysBeforeMin" type="number" min="0" placeholder="Antecedência mín."/><input name="daysBeforeMax" type="number" min="0" placeholder="Antecedência máx."/><input name="startsAt" type="date"/><input name="endsAt" type="date"/><input name="priority" type="number" defaultValue="100" placeholder="Prioridade"/><button style={{background:"#ffd400",border:0,fontWeight:900}}>Criar regra</button></form><div style={{display:"grid",gap:10}}>{rules.map(r=><article key={r.id} style={{border:"1px solid #ddd",borderRadius:14,padding:18}}><b>{r.name}</b><p>{r.accommodation.name} • {r.adjustmentType==="PERCENT"?r.adjustmentValue+"%":(r.adjustmentValue/100).toLocaleString("pt-BR",{style:"currency",currency:"BRL"})} • prioridade {r.priority}</p><small>Ocupação {r.minOccupancyPct??0}%–{r.maxOccupancyPct??100}% • antecedência {r.daysBeforeMin??0}–{r.daysBeforeMax??"∞"} dias</small><form action={deleteRateRule} style={{marginTop:10}}><input type="hidden" name="id" value={r.id}/><button>Excluir regra</button></form></article>)}</div></main>}
