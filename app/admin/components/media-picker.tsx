export default function MediaPicker({name,media,defaultValue=""}:{name:string;media:{id:string;url:string;alt:string|null}[];defaultValue?:string}){return <label style={{display:"grid",gap:8}}>Imagem<select name={name} defaultValue={defaultValue} style={{padding:14}}><option value="">Sem imagem</option>{defaultValue&&!media.some(m=>m.url===defaultValue)&&<option value={defaultValue}>Imagem atual</option>}{media.map(m=><option key={m.id} value={m.url}>{m.alt||m.url}</option>)}</select><small>Escolha uma imagem da Galeria / S3.</small></label>}