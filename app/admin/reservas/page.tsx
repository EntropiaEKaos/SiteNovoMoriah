import {prisma} from "../../../lib/prisma";import {setBookingStatus} from "../actions";export const dynamic="force-dynamic";const labels={NEW:"Nova",CONTACTED:"Contatada",CONFIRMED:"Confirmada",CANCELLED:"Cancelada"} as const;export default async function Page(){const leads=await prisma.bookingLead.findMany({orderBy:{createdAt:"desc"},take:100});return <main style={{padding:"50px 6vw"}}><small>MORIAH CMS / OPERAÇÃO</small><h1 style={{fontSize:48}}>Reservas</h1>{leads.length===0?<div style={{border:"1px dashed #bbb",padding:60,textAlign:"center"}}><h2>Nenhuma solicitação ainda</h2><p>As reservas enviadas pelo site aparecerão aqui.</p></div>:<div style={{display:"grid",gap:14}}>{leads.map(x=><article key={x.id} style={{border:"1px solid #ddd",padding:22,background:"#fff"}}><div style={{display:"flex",justifyContent:"space-between",gap:20,flexWrap:"wrap"}}><div><b style={{fontSize:20}}>{x.name}</b><p>{x.phone}{x.email?" • "+x.email:""}</p><small>{x.guests} hóspede(s) • {x.checkIn?x.checkIn.toLocaleDateString("pt-BR"):"sem check-in"} → {x.checkOut?x.checkOut.toLocaleDateString("pt-BR"):"sem check-out"}</small></div><form action={setBookingStatus}><input type="hidden" name="id" value={x.id}/><select name="status" defaultValue={x.status} style={{padding:12}}>{Object.entries(labels).map(([v,l])=><option key={v} value={v}>{l}</option>)}</select><button style={{padding:12,background:"#ffd400",border:0,fontWeight:800}}>Atualizar</button></form></div>{x.message&&<p style={{marginTop:18,color:"#666"}}>{x.message}</p>}</article>)}</div>}</main>}