import Link from "next/link";import {notFound} from "next/navigation";import {prisma} from "../../../../lib/prisma";import {requireAdmin} from "../../../../lib/admin-auth";import EventForm from "../event-form";import {deleteEvent,saveEvent} from "../actions";export const dynamic="force-dynamic";
export default async function Page({params}:{params:Promise<{id:string}>}){await requireAdmin();const {id}=await params;const [event,media]=await Promise.all([prisma.moriahEvent.findUnique({where:{id}}),prisma.media.findMany({select:{id:true,url:true,alt:true},orderBy:{createdAt:"desc"},take:250})]);if(!event)notFound();return <main className="adminPage"><section className="adminPageHero"><div><small>MORIAH CMS / EVENTO</small><h1>{event.title}</h1></div><div className="adminPageHeroActions"><Link className="adminSecondaryAction" href="/admin/eventos">← Eventos</Link>{event.published&&<Link className="adminPrimaryAction" href={"/eventos/"+event.slug} target="_blank">Ver no site ↗</Link>}</div></section><section className="adminSectionCard"><EventForm action={saveEvent} event={event} media={media} submitLabel="Salvar evento"/><form action={deleteEvent} style={{marginTop:18}}><input type="hidden" name="id" value={event.id}/><button className="danger">Excluir evento definitivamente</button></form></section></main>}
