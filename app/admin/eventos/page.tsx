import Link from "next/link";import {prisma} from "../../../lib/prisma";import {requireAdmin} from "../../../lib/admin-auth";import EventForm from "./event-form";import {createEvent,deleteEvent} from "./actions";export const dynamic="force-dynamic";
export default async function Page(){await requireAdmin();const [events,media]=await Promise.all([prisma.moriahEvent.findMany({orderBy:[{startsAt:"desc"},{createdAt:"desc"}]}),prisma.media.findMany({select:{id:true,url:true,alt:true},orderBy:{createdAt:"desc"},take:250})]);const now=new Date();return <main className="adminPage"><section className="adminPageHero"><div><small>MORIAH CMS / EXPERIÊNCIAS</small><h1>Eventos</h1><p>Crie experiências, publique no site e transforme a Roleta 2.0 conforme as datas.</p></div><div className="adminPageHeroActions"><Link className="adminSecondaryAction" href="/eventos" target="_blank">Ver eventos ↗</Link></div></section><section className="adminMetricStrip"><div><small>Eventos</small><strong>{events.length}</strong></div><div><small>Publicados</small><strong>{events.filter(x=>x.published).length}</strong></div><div><small>Atuais / futuros</small><strong>{events.filter(x=>(x.endsAt||x.startsAt)>=now).length}</strong></div><div><small>Temas roleta</small><strong>{events.filter(x=>x.rouletteThemeEnabled).length}</strong></div></section><section className="adminTwoCol"><article className="adminSectionCard"><h2>Novo evento</h2><EventForm action={createEvent} media={media} submitLabel="Criar evento"/></article><aside className="adminSectionCard isDark"><small>EVENTOS 1.0</small><h2>Do painel para o site.</h2><p>Conteúdo, galeria, CTA, destaque, publicação e identidade sazonal da roleta em um só lugar.</p></aside></section><section className="adminStack" style={{marginTop:24}}>{events.map(e=><article className="adminListCard" key={e.id}><div className="adminListCardHead"><div><small>{e.category} • {e.startsAt.toLocaleString("pt-BR")}</small><h3>{e.title}</h3><p>{e.summary||e.description.slice(0,180)}</p></div><span className={"adminChip "+(e.published?"ok":"warn")}>{e.published?"NO AR":"RASCUNHO"}</span></div><div className="adminMetaRow">{e.featured&&<span className="adminChip">DESTAQUE</span>}{e.showOnHome&&<span className="adminChip">HOME</span>}{e.rouletteThemeEnabled&&<span className="adminChip">ROLETA: {e.themePreset}</span>}</div><div className="adminInlineActions"><Link className="highlight" href={"/admin/eventos/"+e.id}>Editar</Link>{e.published&&<Link href={"/eventos/"+e.slug} target="_blank">Ver ↗</Link>}<form action={deleteEvent}><input type="hidden" name="id" value={e.id}/><button className="danger">Excluir</button></form></div></article>)}</section></main>}
