import {prisma} from "../../../lib/prisma";import {requireAdmin} from "../../../lib/admin-auth";import {createPromotion,togglePromotion,deletePromotion} from "../actions";export const dynamic="force-dynamic";
const money=(v:number|null,t:string|null)=>v==null?"Institucional":t==="FIXED"?(v/100).toLocaleString("pt-BR",{style:"currency",currency:"BRL"}):v+"%";
export default async function Page(){await requireAdmin();const [rows,rooms]=await Promise.all([prisma.promotion.findMany({orderBy:{createdAt:"desc"}}),prisma.accommodation.findMany({where:{active:true},orderBy:{name:"asc"}})]);return <main style={{padding:"52px 6vw",maxWidth:1180,margin:"0 auto",background:"#fafafa",minHeight:"100vh"}}><small>MORIAH CMS / REVENUE & MARKETING</small><h1 style={{fontSize:"clamp(38px,6vw,64px)",margin:"8px 0"}}>Promoções inteligentes</h1><p style={{maxWidth:720,color:"#555"}}>Crie campanhas que entram diretamente na cotação oficial. Cupons podem ser exclusivos, limitar estadia mínima e valer para uma hospedagem específica.</p><form action={createPromotion} style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(180px,1fr))",gap:12,background:"#111",color:"#fff",padding:24,borderRadius:20,margin:"28px 0"}}><input name="title" required placeholder="Nome da campanha"/><input name="coupon" placeholder="Cupom (opcional)"/><select name="discountType" required><option value="PERCENT">Desconto %</option><option value="FIXED">Desconto R$</option></select><input name="discountValue" type="number" step=".01" min=".01" required placeholder="Valor"/><input name="minNights" type="number" min="1" placeholder="Mínimo de noites"/><select name="accommodationId"><option value="">Todas as hospedagens</option>{rooms.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select><input name="description" placeholder="Descrição comercial"/><label style={{display:"flex",alignItems:"center",gap:8}}><input name="stackable" type="checkbox"/> Permitir combinar</label><button style={{background:"#ffd400",border:0,padding:14,fontWeight:900,borderRadius:10}}>Criar promoção</button></form><section style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(270px,1fr))",gap:16}}>{rows.map(p=><article key={p.id} style={{background:"#fff",border:"1px solid #e5e5e5",borderRadius:18,padding:22,boxShadow:"0 8px 30px rgba(0,0,0,.04)"}}><div style={{display:"flex",justifyContent:"space-between",gap:10}}><b style={{fontSize:20}}>{p.title}</b><span style={{fontSize:12,fontWeight:900}}>{p.active?"ATIVA":"PAUSADA"}</span></div><div style={{fontSize:32,fontWeight:900,margin:"18px 0"}}>{money(p.discountValue,p.discountType)} OFF</div><p style={{color:"#666"}}>{p.description||"Campanha comercial Moriah"}</p><small>{p.coupon?"Cupom "+p.coupon+" • ":""}{p.minNights?"mín. "+p.minNights+" noites • ":""}{p.stackable?"combinável":"não combinável"}</small><div style={{display:"flex",gap:8,marginTop:18}}><form action={togglePromotion}><input type="hidden" name="id" value={p.id}/><button>{p.active?"Pausar":"Ativar"}</button></form><form action={deletePromotion}><input type="hidden" name="id" value={p.id}/><button style={{background:"#111",color:"#fff"}}>Excluir</button></form></div></article>)}</section></main>}