import {firebaseConfig,firebaseConfigured} from "../../../lib/firebase";
export const dynamic="force-dynamic";
export default function Page(){const checks=[["API key",firebaseConfig.apiKey],["Auth domain",firebaseConfig.authDomain],["Project ID",firebaseConfig.projectId],["Storage bucket",firebaseConfig.storageBucket],["Messaging sender",firebaseConfig.messagingSenderId],["App ID",firebaseConfig.appId],["VAPID",process.env.NEXT_PUBLIC_FIREBASE_VAPID_KEY]];return <main style={{padding:"50px 6vw",maxWidth:1000}}><small>MORIAH CMS / INTEGRAÇÕES</small><h1 style={{fontSize:48}}>Integrações</h1><section style={{marginTop:24,padding:28,background:"#111",color:"#fff"}}><h2>Firebase</h2><p>{firebaseConfigured?"Configuração base carregada.":"Configuração incompleta. O site continua funcional; complete as variáveis na Vercel para ativar os recursos Firebase."}</p><div style={{display:"grid",gap:8,marginTop:18}}>{checks.map(([name,value])=><div key={name} style={{display:"flex",justifyContent:"space-between",borderBottom:"1px solid #333",padding:"9px 0"}}><span>{name}</span><b style={{color:value?"#ffd400":"#ff8a8a"}}>{value?"CONFIGURADO":"PENDENTE"}</b></div>)}</div><p style={{marginTop:20}}>Segredos e valores não são exibidos no painel. A fonte de configuração é o ambiente da Vercel.</p></section></main>}