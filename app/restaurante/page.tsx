import {prisma} from "../../lib/prisma";import Menu from "./menu";export const dynamic="force-dynamic";export default async function Page(){const products=await prisma.restaurantProduct.findMany({where:{active:true,category:{active:true}},include:{category:true},orderBy:[{category:{sortOrder:"asc"}},{name:"asc"}]});const available=products.filter(p=>!p.trackStock||p.stockQty>0).map(p=>({id:p.id,name:p.name,description:p.description,priceCents:p.priceCents,stockQty:p.stockQty,trackStock:p.trackStock,category:p.category.name}));return <main style={{padding:"44px 5vw",maxWidth:1400,margin:"0 auto",background:"#faf9f5",minHeight:"100vh"}}><small>MORIAH FOOD • PEÇA SEM SAIR DA POUSADA</small><h1 style={{fontSize:"clamp(46px,8vw,86px)",lineHeight:.95,margin:"12px 0 28px"}}>Seu pedido,<br/>direto da cozinha.</h1><Menu products={available}/></main>}