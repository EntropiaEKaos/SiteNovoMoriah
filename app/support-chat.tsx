"use client";import {useState} from "react";import {Bot,Send,X} from "lucide-react";
type Msg={role:"user"|"assistant";content:string};
export default function SupportChat(){const [open,setOpen]=useState(false),[text,setText]=useState(""),[busy,setBusy]=useState(false),[messages,setMessages]=useState<Msg[]>([{role:"assistant",content:"Olá! Sou o assistente virtual da Moriah. Como posso ajudar com sua hospedagem?"}]);async function send(e:React.FormEvent){e.preventDefault();const value=text.trim();if(!value||busy)return;const next=[...messages,{role:"user" as const,content:value}];setMessages(next);setText("");setBusy(true);try{const r=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({messages:next})});const d=await r.json();setMessages(m=>[...m,{role:"assistant",content:d.reply||d.error||"Atendimento indisponível no momento."}])}catch{setMessages(m=>[...m,{role:"assistant",content:"Não consegui conectar agora. Tente novamente em instantes."}])}finally{setBusy(false)}}return <div className="supportChat"><button className="chatLauncher" onClick={()=>setOpen(v=>!v)} aria-label="Abrir atendimento virtual">{open?<X/>:<Bot/>}<span>Atendimento</span></button>{open&&<section className="chatWindow"><header><div><b>Moriah Assistente</b><small>IA para dúvidas rápidas</small></div><button onClick={()=>setOpen(false)} aria-label="Fechar"><X size={18}/></button></header><div className="chatMessages">{messages.map((m,i)=><p key={i} className={m.role}>{m.content}</p>)}{busy&&<p className="assistant">Digitando…</p>}</div><form onSubmit={send}><input value={text} onChange={e=>setText(e.target.value)} maxLength={1500} placeholder="Digite sua dúvida…" aria-label="Mensagem"/><button disabled={busy||!text.trim()} aria-label="Enviar"><Send size={18}/></button></form><small className="chatDisclaimer">Disponibilidade e reservas são confirmadas pelo sistema/equipe.</small></section>}</div>}