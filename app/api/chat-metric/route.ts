import {NextResponse} from "next/server";import {prisma} from "../../../lib/prisma";const allowed=new Set(["OPEN","MESSAGE","BOOKING_CTA","WHATSAPP"]);export async function POST(req:Request){try{const {kind}=await req.json();if(!allowed.has(kind))return NextResponse.json({ok:false},{status:400});await prisma.chatMetric.create({data:{kind}});return NextResponse.json({ok:true})}catch{return NextResponse.json({ok:false},{status:400})}}