import {NextRequest,NextResponse} from "next/server";import {isAdmin} from "../../../../lib/admin-auth";import {createMediaUpload} from "../../../../lib/media-storage";export async function POST(req:NextRequest){if(!(await isAdmin()))return NextResponse.json({error:"unauthorized"},{status:401});try{const b=await req.json();const result=await createMediaUpload({mimeType:String(b.mimeType||""),size:Number(b.size||0),extension:String(b.extension||"")});return NextResponse.json(result)}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"upload error"},{status:400})}}