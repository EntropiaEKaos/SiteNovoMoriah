import {NextRequest,NextResponse} from "next/server";import {prisma} from "../../../../lib/prisma";export const dynamic="force-dynamic";function esc(v:string){return v.replace(/\\/g,"\\\\").replace(/,/g,"\\,").replace(/;/g,"\\;").replace(/\n/g,"\\n")}function stamp(d:Date){return d.toISOString().replace(/[-:]/g,"").replace(/\.\d{3}Z$/,"Z")}export async function GET(_req:NextRequest,{params}:{params:Promise<{token:string}>}){const {token}=await params;const integration=await prisma.channelIntegration.findUnique({where:{exportToken:token},select:{id:true,active:true,accommodationId:true}});if(!integration?.active||!integration.accommodationId)return new NextResponse("Not found",{status:404});const rows=await prisma.bookingLead.findMany({where:{accommodationId:integration.accommodationId,status:{in:["CONFIRMED","CHECKED_IN"]},checkIn:{not:null},checkOut:{not:null}},select:{id:true,checkIn:true,checkOut:true}});const lines=["BEGIN:VCALENDAR","VERSION:2.0","PRODID:-//Pousada Moriah//Reservas//PT","CALSCALE:GREGORIAN",...rows.flatMap(r=>["BEGIN:VEVENT","UID:"+esc(r.id+"@moriah"),"DTSTAMP:"+stamp(new Date()),"DTSTART:"+stamp(r.checkIn!),"DTEND:"+stamp(r.checkOut!),"SUMMARY:Moriah - indisponível","END:VEVENT"]),"END:VCALENDAR"];return new NextResponse(lines.join("\r\n"),{headers:{"content-type":"text/calendar; charset=utf-8","cache-control":"no-store"}})}