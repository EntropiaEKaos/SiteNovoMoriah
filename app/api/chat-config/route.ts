import {NextResponse} from "next/server";import {prisma} from "../../../lib/prisma";export const dynamic="force-dynamic";export async function GET(){const x=await prisma.integrationSettings.findUnique({where:{id:"main"},select:{chatEnabled:true,chatName:true,chatWelcome:true}});return NextResponse.json({enabled:x?.chatEnabled!==false,name:x?.chatName||"Moriah Assistente",welcome:x?.chatWelcome||"Olá! Sou o assistente virtual da Moriah. Como posso ajudar com sua hospedagem?"},{headers:{"Cache-Control":"no-store"}})}