import {NextResponse} from "next/server";
const URL="https://api.groq.com/openai/v1/chat/completions";
const SYSTEM=`Você é o assistente virtual da Pousada Moriah, em Praia Grande, SP. Responda em português do Brasil, de forma acolhedora, objetiva e curta. Ajude com dúvidas gerais sobre hospedagem e oriente o visitante a usar a página /reservar para disponibilidade e solicitação de reserva. Nunca invente preços, disponibilidade, políticas, endereço exato ou serviços. Se a informação não estiver no contexto fornecido, diga que precisa ser confirmada pela equipe. Nunca peça dados sensíveis ou dados de cartão.`;
export async function POST(req:Request){const key=process.env.GROQ_API_KEY;if(!key)return NextResponse.json({error:"Atendimento por IA ainda não configurado."},{status:503});let body:any;try{body=await req.json()}catch{return NextResponse.json({error:"Requisição inválida."},{status:400})}const raw=Array.isArray(body?.messages)?body.messages:[];const messages=raw.slice(-10).filter((m:any)=>["user","assistant"].includes(m?.role)&&typeof m?.content==="string").map((m:any)=>({role:m.role,content:m.content.slice(0,1500)}));if(!messages.length)return NextResponse.json({error:"Mensagem obrigatória."},{status:400});try{const res=await fetch(URL,{method:"POST",headers:{"Content-Type":"application/json","Authorization":`Bearer ${key}`},body:JSON.stringify({model:process.env.GROQ_CHAT_MODEL||"llama-3.1-8b-instant",temperature:.35,max_completion_tokens:450,messages:[{role:"system",content:SYSTEM},...messages]}),signal:AbortSignal.timeout(15000)});if(!res.ok){console.error("Groq chat failed",res.status);return NextResponse.json({error:"Atendimento temporariamente indisponível."},{status:502})}const data=await res.json();const reply=data?.choices?.[0]?.message?.content?.trim();return NextResponse.json({reply:reply||"Não consegui responder agora. Tente novamente em instantes."})}catch{return NextResponse.json({error:"Atendimento temporariamente indisponível."},{status:502})}}