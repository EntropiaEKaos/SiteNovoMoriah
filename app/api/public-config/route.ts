import {NextResponse} from "next/server";import {prisma} from "../../../lib/prisma";
export const dynamic="force-dynamic";
export async function GET(){const row=await prisma.integrationSettings.findUnique({where:{id:"main"}});const db={apiKey:row?.firebaseApiKey,authDomain:row?.firebaseAuthDomain,projectId:row?.firebaseProjectId,storageBucket:row?.firebaseStorageBucket,messagingSenderId:row?.firebaseMessagingSenderId,appId:row?.firebaseAppId,vapidKey:row?.firebaseVapidKey};const env={apiKey:process.env.NEXT_PUBLIC_FIREBASE_API_KEY,authDomain:process.env.NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN,projectId:process.env.NEXT_PUBLIC_FIREBASE_PROJECT_ID,storageBucket:process.env.NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET,messagingSenderId:process.env.NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID,appId:process.env.NEXT_PUBLIC_FIREBASE_APP_ID,vapidKey:process.env.NEXT_PUBLIC_FIREBASE_VAPID_KEY};const config=Object.fromEntries(Object.entries(db).map(([k,v])=>[k,v||env[k as keyof typeof env]||null]));return NextResponse.json({configured:["apiKey","authDomain","projectId","storageBucket","messagingSenderId","appId"].every(k=>Boolean(config[k])),config},{headers:{"Cache-Control":"no-store"}})}