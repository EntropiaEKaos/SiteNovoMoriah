"use client";import {useMemo,useState} from "react";import {createBookingLead} from "../admin/actions";type Room={id:string;name:string};export default function BookingCalendar({rooms,selected}:{rooms:Room[];selected:string}){const [room,setRoom]=useState(selected);const [start,setStart]=useState("");const [end,setEnd]=useState("");const min=useMemo(()=>new Date().toISOString().slice(0,10),[]);return <section style={{maxWidth:1000,background:"#fff",padding:28,border:"1px solid #ddd"}}><h2>Escolha sua hospedagem e período</h2><form action={createBookingLead} style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(210px,1fr))",gap:14}}><select name="accommodationId" required value={room} onChange={e=>setRoom(e.target.value)} style={{padding:15}}><option value="">Escolha a hospedagem</option>{rooms.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select><label>Entrada<input name="checkIn" type="date" min={min} required value={start} onChange={e=>{setStart(e.target.value);if(end&&end<=e.target.value)setEnd("")}} style={{padding:15,width:"100%"}}/></label><label>Saída<input name="checkOut" type="date" min={start||min} required value={end} onChange={e=>setEnd(e.target.value)} style={{padding:15,width:"100%"}}/></label><input name="name" required placeholder="Seu nome" style={{padding:15}}/><input name="phone" required placeholder="WhatsApp" style={{padding:15}}/><input name="email" type="email" placeholder="E-mail" style={{padding:15}}/><input name="guests" type="number" min="1" defaultValue="1" style={{padding:15}}/><textarea name="message" placeholder="Observações" style={{padding:15,gridColumn:"1/-1"}}/><button disabled={!room||!start||!end} style={{border:0,background:"#ffd400",fontWeight:800,padding:16}}>Solicitar reserva</button></form><p style={{marginTop:14,fontSize:14}}>A disponibilidade é validada novamente no servidor antes de registrar a solicitação.</p></section>}