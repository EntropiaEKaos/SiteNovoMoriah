"use client";import {useEffect,useMemo,useState} from "react";import {createBookingLead} from "../admin/actions";type Room={id:string;name:string};export default function BookingCalendar({rooms,selected}:{rooms:Room[];selected:string}){const [room,setRoom]=useState(selected);const [start,setStart]=useState("");const [end,setEnd]=useState("");const [blocks,setBlocks]=useState<{start:string,end:string}[]>([]);const [loading,setLoading]=useState(false);const min=useMemo(()=>new Date().toISOString().slice(0,10),[]);useEffect(()=>{if(!room){setBlocks([]);return}let live=true;setLoading(true);fetch("/api/availability?accommodationId="+encodeURIComponent(room)).then(r=>r.ok?r.json():Promise.reject()).then(d=>{if(live)setBlocks(Array.isArray(d.blocks)?d.blocks:[])}).catch(()=>{if(live)setBlocks([])}).finally(()=>{if(live)setLoading(false)});return()=>{live=false}},[room]);const blocked=(day:string)=>blocks.some(b=>b.start<=day&&b.end>day);const rangeBlocked=start&&end&&blocks.some(b=>b.start<end&&b.end>start);return <section style={{maxWidth:1000,background:"#fff",padding:28,border:"1px solid #ddd"}}><h2>Escolha sua hospedagem e período</h2><div aria-live="polite" style={{marginBottom:16}}>{loading?"Consultando calendário…":room?<><b>Disponibilidade carregada.</b> {blocks.length} período(s) ocupado(s) nos próximos meses.</>:"Escolha uma hospedagem para consultar a disponibilidade."}</div><form action={createBookingLead} style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(210px,1fr))",gap:14}}><select name="accommodationId" required value={room} onChange={e=>setRoom(e.target.value)} style={{padding:15}}><option value="">Escolha a hospedagem</option>{rooms.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select><label>Entrada<input name="checkIn" type="date" min={min} required value={start} onChange={e=>{setStart(e.target.value);if(end&&end<=e.target.value)setEnd("")}} style={{padding:15,width:"100%"}}/></label><label>Saída<input name="checkOut" type="date" min={start||min} required value={end} onChange={e=>setEnd(e.target.value)} style={{padding:15,width:"100%"}}/></label><input name="name" required placeholder="Seu nome" style={{padding:15}}/><input name="phone" required placeholder="WhatsApp" style={{padding:15}}/><input name="email" type="email" placeholder="E-mail" style={{padding:15}}/><input name="guests" type="number" min="1" defaultValue="1" style={{padding:15}}/><textarea name="message" placeholder="Observações" style={{padding:15,gridColumn:"1/-1"}}/><button disabled={!room||!start||!end||rangeBlocked||blocked(start)} style={{border:0,background:"#ffd400",fontWeight:800,padding:16}}>Solicitar reserva</button></form>{rangeBlocked&&<p style={{padding:12,background:"#fff3cd",border:"1px solid #ffd400"}}>O período selecionado cruza datas ocupadas. Escolha outro intervalo.</p>}<details style={{marginTop:14}}><summary>Ver períodos já ocupados</summary><ul>{blocks.map((b,i)=><li key={i}>{new Date(b.start+"T12:00:00").toLocaleDateString("pt-BR")} → {new Date(b.end+"T12:00:00").toLocaleDateString("pt-BR")}</li>)}</ul></details><p style={{marginTop:14,fontSize:14}}>A disponibilidade é validada novamente no servidor antes de registrar a solicitação.</p></section>}