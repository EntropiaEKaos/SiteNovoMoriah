import Link from "next/link";import {ArrowRight,CalendarDays,MapPin,Sparkles} from "lucide-react";import {prisma} from "../../lib/prisma";import {loadPublicSiteSettings} from "../../lib/public-site-settings";import PublicSiteChrome from "../public-site-chrome";export const dynamic="force-dynamic";
export default async function Page(){const [events,settings,navPages]=await Promise.all([prisma.moriahEvent.findMany({where:{published:true},orderBy:[{featured:"desc"},{startsAt:"asc"}]}),loadPublicSiteSettings(),prisma.sitePage.findMany({where:{published:true,showInNav:true,slug:{not:"home"}},select:{slug:true,title:true,navLabel:true},orderBy:[{sortOrder:"asc"},{createdAt:"asc"}],take:6})]);const now=new Date(),up=events.filter(e=>(e.endsAt||new Date(e.startsAt.getTime()+86400000))>=now);return <PublicSiteChrome settings={settings} navPages={navPages}><section className="eventsArchiveV7"><header className="eventsArchiveHeroV7"><small>MORIAH EXPERIENCE</small><h1>Viva a agenda da Moriah.</h1><p>Gastronomia, encontros, música e experiências sazonais para hóspedes e comunidade.</p></header><div className="eventsArchiveGridV7">{up.map(e=><article className="eventsArchiveCardV7" key={e.id}><Link href={"/eventos/"+e.slug} className="eventsArchiveMediaV7">{e.coverImage?<img src={e.coverImage} alt={e.title}/>:<div><Sparkles size={46}/></div>}{e.badge&&<b>{e.badge}</b>}</Link><div className="eventsArchiveBodyV7"><small>{e.category}</small><h2><Link href={"/eventos/"+e.slug}>{e.title}</Link></h2><p>{e.summary||e.description.slice(0,210)}</p><div className="eventsArchiveMetaV7"><span><CalendarDays size={14}/>{e.startsAt.toLocaleString("pt-BR",{dateStyle:"medium",timeStyle:"short"})}</span>{e.venue&&<span><MapPin size={14}/>{e.venue}</span>}</div><Link className="eventsArchiveCtaV7" href={"/eventos/"+e.slug}>Ver experiência <ArrowRight size={16}/></Link></div></article>)}</div>{!up.length&&<div className="eventsEmptyV7"><Sparkles/><h2>Novas experiências em preparação.</h2></div>}</section></PublicSiteChrome>}
