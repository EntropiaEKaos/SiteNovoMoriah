ALTER TABLE "Accommodation"
  ADD COLUMN IF NOT EXISTS "galleryImages" TEXT[] NOT NULL DEFAULT ARRAY[]::TEXT[],
  ADD COLUMN IF NOT EXISTS "internalCode" TEXT,
  ADD COLUMN IF NOT EXISTS "roomNumber" TEXT,
  ADD COLUMN IF NOT EXISTS "floor" TEXT,
  ADD COLUMN IF NOT EXISTS "maxAdults" INTEGER NOT NULL DEFAULT 2,
  ADD COLUMN IF NOT EXISTS "maxChildren" INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "beds" TEXT,
  ADD COLUMN IF NOT EXISTS "bathrooms" INTEGER NOT NULL DEFAULT 1,
  ADD COLUMN IF NOT EXISTS "areaSqm" DOUBLE PRECISION,
  ADD COLUMN IF NOT EXISTS "amenities" TEXT[] NOT NULL DEFAULT ARRAY[]::TEXT[],
  ADD COLUMN IF NOT EXISTS "rules" TEXT,
  ADD COLUMN IF NOT EXISTS "checkInTime" TEXT NOT NULL DEFAULT '14:00',
  ADD COLUMN IF NOT EXISTS "checkOutTime" TEXT NOT NULL DEFAULT '12:00',
  ADD COLUMN IF NOT EXISTS "internalNotes" TEXT;

CREATE UNIQUE INDEX IF NOT EXISTS "Accommodation_internalCode_key"
  ON "Accommodation"("internalCode");
